SIGNATURE Layout

IMPORT Seq            ONLY seq
       String         ONLY string
       Real           ONLY real
       WinConfig      ONLY point
       WinFontMetrics ONLY metrics

       Types          ONLY posWord line
       Syntax         COMPLETELY

FUN computeLayout : seq[par] ** metrics -> line
