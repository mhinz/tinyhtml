SIGNATURE Layout

IMPORT Seq            ONLY seq
       String         ONLY string
       Real           ONLY real
       WinConfig      ONLY point
       WinFontMetrics ONLY metrics

       Types          ONLY posWord words

FUN layout : seq[seq[string]] ** metrics -> words
