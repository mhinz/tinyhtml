IMPLEMENTATION Draw

-- IMPORTS {{{1

IMPORT Seq             COMPLETELY
       SeqMap          COMPLETELY
       Denotation      COMPLETELY
       Com             COMPLETELY
       Void            COMPLETELY
       Nat             COMPLETELY
       Real            COMPLETELY
       String          COMPLETELY
       RealConv        COMPLETELY
       NatConv         COMPLETELY

       ProcessArgs     COMPLETELY
       WinButton       COMPLETELY
       WinCanvas       COMPLETELY
       WinCanvasEditor COMPLETELY
       WinConfig       COMPLETELY
       WinEmitter      COMPLETELY
       WinWindow       COMPLETELY
       WinView         COMPLETELY

       Dot             COMPLETELY
       Settings        COMPLETELY
       Layout          COMPLETELY
       Types           ONLY words

-- GLOBAL FUNCTIONS {{{1

/*
 * Example input.
 */
DEF sample == %(
                %(!("Hinter"), !("dir!")                     ),
                %(!("Ein"),    !("dreikoepfiger"), !("Affe!"))
               )

/*
 * Draw the display.
 */
DEF draw(edit, passages, metr) == LET posWords == layout(passages, metr)
                                  IN display(edit, posWords.wordsToDrawing)

-- LOCAL FUNCTIONS {{{1

FUN wordsToDrawing : words -> drawing
DEF wordsToDrawing (<>)          == {}
DEF wordsToDrawing (words(<>))   == {}
DEF wordsToDrawing (words(w::W)) == wordsToDrawing(words(W)) ++ (text(w.position) with
                                                                (text      (w.text)        ++
                                                                 anchor    (north)         ++
                                                                 font      (font'Settings) ++
                                                                 foreground(foreground'Settings)))
