SIGNATURE Draw

-- IMPORTS {{{1

IMPORT Com             ONLY com
       Void            ONLY void
       String          ONLY string
       Seq             ONLY seq

       WinCanvasEditor ONLY canvasEditor
       WinConfig       ONLY point
       WinFontMetrics  ONLY metrics

       Types           ONLY posWord line

-- EXPORTS {{{1

FUN draw : canvasEditor ** line ** metrics -> com[void]
