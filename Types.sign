SIGNATURE Types

IMPORT Seq       ONLY seq
       String    ONLY string
       Real      ONLY real
       WinConfig ONLY point

TYPE posWord == posWord(text     : string,
                        position : point,
                        width    : real)

TYPE words == words(data: seq[posWord])

FUN << : words ** posWord -> words
